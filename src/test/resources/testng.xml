<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd" >
<suite name="API Suite" verbose="1" parallel="false">
    <parameter name="testPackage" value="${testPackage}"/>

    <listeners>
        <listener class-name="listeners.AllureListener"/>
    </listeners>

    <test name="REST Tests">
        <packages>
            <package name="${testPackage}"/>
        </packages>
    </test>
</suite>

